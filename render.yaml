services:
  - type: web
    name: ntg-travel
    runtime: node
    buildCommand: "npm install"
    startCommand: "npm start"
    envVars:
      - key: ADMIN_PASSWORD
        sync: false
      - key: NODE_ENV
        value: production
